# 개발용 목록이다. 제출본을 받은 PC에서는 `setup_env.py`가 이 파일을 쓰지 않고
# 자체 목록으로 전용 가상환경(.venv)에 깐다 — torch를 CPU 판으로 받고, 실패해도
# 되는 것(OCR)과 아닌 것을 나눠야 해서 pip 한 줄로는 안 되기 때문이다.
# 여기 목록을 고치면 setup_env.py 쪽도 같이 봐야 한다.

# 웹 서버 / 데스크톱 창
fastapi>=0.110
uvicorn[standard]>=0.29
pywebview>=5.1
websockets>=12.0
requests>=2.30
itsdangerous>=2.0   # 웹 로그인 세션(SessionMiddleware). serve_web.py에서 사용
python-multipart>=0.0.9  # 로그인 폼 + 영상 업로드(multipart) 처리에 필요
pymupdf>=1.24        # 파이프에셋 야장 PDF 생성(backend/pipeasset_pdf.py)

# 아래 패키지는 개발 환경(Python 3.13)에 이미 설치되어 있음 - 없다면 함께 설치
# (Colab 원격 추론 서버를 쓸 경우 로컬에는 ultralytics/torch가 없어도 동작함)
ultralytics>=8.4
opencv-python>=4.12
paddlepaddle>=3.0
paddleocr>=3.0
openpyxl>=3.1
pillow>=11.0
torch>=2.8

# Stage-1 결함 필터(backend/defect_filter.py). torch 없이 돌리기 위해 ONNX를 쓴다.
onnxruntime>=1.18

# LLM 판독(backend/llm_infer.py) — 선택. 외부 API를 부르므로 회사망에서는 못 쓴다.
# 앱 화면에서 껐다 켤 수 있고, 꺼져 있으면 이 패키지들이 없어도 정상 동작한다.
google-generativeai>=0.8
openai>=1.40
